package com.instisys.prime.repository;

import com.instisys.prime.entity.Fees;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FeesRepository extends JpaRepository<Fees, Long> {
}
